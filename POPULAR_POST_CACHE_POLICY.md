# 인기글 캐시 정책

이 문서는 인기글 캐시의 적용 범위, 만료, 목록 교체, 변경 이벤트별 무효화 규칙을 빠르게 확인하기 위한 운영 기준이다.

## 1. 핵심 원칙

1. 캐시는 현재 인기 목록 상위 10개에만 적용한다.
2. 인기 후보 조건은 목록 생성 시에만 검사한다.
3. 상세·댓글 요청의 캐시 판정은 현재 스냅샷의 `postNums.contains(postNum)`으로 끝낸다.
4. 목록 갱신은 TTL 만료가 아니라 5분 집계 완료 후 새 스냅샷 교체로 수행한다.
5. 본문·댓글은 변경 시 무효화하고, 상태는 최신성을 일부 포기해 1분 TTL만 사용한다.
6. 블라인드는 접근 제어이므로 상태 TTL 정책의 예외로 즉시 반영한다.
7. 모든 변경 이벤트 무효화는 트랜잭션 커밋 후 실행한다.
8. 캐시는 JPA 엔티티가 아닌 불변 DTO만 저장한다.

## 2. 캐시 범위

| 캐시 | 키 | 저장 값 | 적용 범위 | 만료/갱신 | 용량 제한 |
|---|---|---|---|---|---|
| 인기 목록 스냅샷 | singleton | 정렬된 안정 요약 + `postNum Set` | 상위 10개 목록 | 집계 완료 시 원자적 교체, 안전 TTL 15분 | 1개 |
| 인기글 본문 | `postNum` | 작성자·제목·본문·이미지·시각 DTO | 현재 인기글 상세 | access 10분, write 30분 | 약 4 MiB weight |
| 인기글 상태 | `postNum` | 조회·좋아요·신고·댓글 수 | 현재 인기글 상세 | write 1분 | 최대 20개 |
| 댓글 첫 페이지 인덱스 | `postNum` | 정렬된 댓글 ID + `totalCount` | 인기글 `page=0,size=10` | access 10분, write 30분 | 최대 20개 |
| 개별 댓글 | `commentNum` | 댓글 표시 DTO | 캐시된 인기글 첫 페이지 댓글 | access 10분, write 30분 | 약 8 MiB weight |

### 인기 목록에 저장하지 않는 값

인기 목록 화면에서 사용하지 않으므로 다음 상태 값은 목록 스냅샷과 목록 API에서 제외한다.

```text
viewCount
likeCount
reportCount
commentCount
```

`reportCount`는 상태 캐시 내부와 관리자 API에서만 사용한다. 공개 게시글 API는 실제 신고 수 대신 백엔드가 계산한 `blind`만 제공하며, 인기 목록은 블라인드 게시글을 제외하므로 `blind`도 포함하지 않는다.

## 3. 인기 판정

### 목록 생성 시 검사

```text
작성 시각이 후보 기간 72시간 이내
deletedAt IS NULL
reportCount <= 5
인기 점수 상위 10개
```

### 요청 시 검사

```java
snapshot.postNums().contains(postNum)
```

요청 시 후보 기간, 게시글 테이블, 인기 통계 테이블을 추가 조회하지 않는다. 현재 목록이 5분 스냅샷이므로 72시간 경계 통과도 다음 목록 교체 때 반영한다.

변경 이벤트에서 목록 캐시가 없는 경우 새 스냅샷을 로딩하지 않는다. `getIfPresent()` 결과가 있을 때만 membership을 검사한다.

## 4. 5분 목록 교체 정책

| 분류 | 처리 |
|---|---|
| 이전과 새 목록에 모두 포함 | 이미 존재하는 본문·댓글 캐시만 touch, 상태 TTL은 건드리지 않음 |
| 새 목록에만 포함 | 목록에만 추가, 본문·상태·댓글 선조회 금지 |
| 이전 목록에만 포함 | 본문·상태·댓글 인덱스·연결된 개별 댓글 제거 |

목록은 먼저 invalidate한 뒤 재생성하지 않는다. 새 스냅샷을 완성한 다음 한 번에 교체하여 빈 캐시 구간을 만들지 않는다.

목록 TTL 15분은 5분 갱신 타이머가 아니다. 집계 실패나 스케줄러 누락에 대비한 안전 만료다.

## 5. 변경 이벤트별 무효화 매트릭스

`항상 제거`는 키가 없어도 조회 없이 invalidate한다는 뜻이다. `인기 시 제거`는 현재 메모리의 스냅샷이 해당 `postNum`을 포함할 때만 목록을 제거한다는 뜻이다.

| 이벤트 | 목록 | 본문 | 상태 | 댓글 인덱스 | 개별 댓글 | 비고 |
|---|---|---|---|---|---|---|
| 게시글 수정 | 인기 시 제거 | 해당 글 항상 제거 | 유지 | 유지 | 유지 | 목록 제목과 상세 본문 변경 반영 |
| 게시글 삭제 | 인기 시 제거 | 해당 글 항상 제거 | 해당 글 항상 제거 | 해당 글 항상 제거 | 해당 글 댓글 제거 | 삭제 글 노출 방지 |
| 조회수 증가 | 유지 | 유지 | 유지 | 유지 | 유지 | 상태는 1분 TTL로 수렴 |
| 좋아요/취소 | 유지 | 유지 | 유지 | 유지 | 유지 | mutation 응답은 프론트 낙관적 UI 반영 |
| 신고 1~5회 | 유지 | 유지 | 유지 | 유지 | 유지 | 상태는 1분 TTL로 수렴 |
| 신고 6회 도달 | 인기 시 제거 | 해당 글 항상 제거 | 해당 글 항상 제거 | 해당 글 항상 제거 | 해당 글 댓글 제거 | 블라인드 즉시 반영 |
| 루트 댓글 등록 | 유지 | 유지 | 유지 | 해당 글 제거 | 새 댓글은 필요 시 적재 | ID 목록과 페이지 메타데이터 변경 |
| 댓글 내용 수정 | 유지 | 유지 | 유지 | 유지 | 해당 댓글 제거 | 페이지 순서 불변 |
| 루트 댓글 삭제 | 유지 | 유지 | 유지 | 유지 | 해당 댓글 제거 | 소프트 삭제 댓글은 마스킹되어 목록에 유지 |
| 답글 등록 | 유지 | 유지 | 유지 | 유지 | 부모 댓글 제거 | 부모 `childCount` 변경 |
| 답글 수정 | 유지 | 유지 | 유지 | 유지 | 해당 답글 제거 | 캐시 범위에 존재하는 경우만 효과 발생 |
| 답글 삭제 | 유지 | 유지 | 유지 | 유지 | 해당 답글과 부모 댓글 제거 | 부모 `childCount` 변경 |
| 프로필 수정 | 제거 | 전체 제거 | 유지 | 전체 제거 | 전체 제거 | 캐시가 작으므로 단순 전체 제거 |
| 회원 탈퇴 | 제거 | 전체 제거 | 유지 | 전체 제거 | 전체 제거 | 작성자 마스킹 정보 반영 |

게시글의 댓글 수는 `PostState`에 포함되므로 댓글 등록·삭제 시에도 상태 캐시는 제거하지 않는다. 최대 1분 동안 이전 수치를 허용한다.

## 6. 댓글 캐시 조립 규칙

```text
첫 페이지 인덱스 미스
→ Page 쿼리 1회
→ commentNum 목록과 totalCount를 인덱스에 저장
→ 조회된 댓글을 commentNum별로 저장

첫 페이지 인덱스 히트
→ commentNum별 캐시 조회
→ 누락 ID를 단일 IN 쿼리로 조회
→ 인덱스 순서대로 응답 조립
```

`page=0`, `pageSize=10`은 고정 상수다. `commentCount`는 ID 목록 크기로, `totalPage`는 `ceil(totalCount / 10)`으로 계산한다.

개별 댓글 캐시는 삭제 댓글을 저장할 수 있어야 한다. 삭제 댓글은 존재하지 않는 댓글로 처리하지 않고 마스킹된 표시 DTO로 반환한다.

게시글이 인기 목록에서 이탈할 때 첫 페이지 인덱스의 댓글 ID를 사용해 연결된 개별 댓글을 제거한 뒤 인덱스를 제거한다.

## 7. 만료와 touch 의미

### 목록에 유지된 인기글

5분 목록 교체 때 본문과 댓글 캐시에 값이 이미 있는 경우에만 읽기 touch를 수행한다. 이 작업은 `expireAfterAccess=10분`을 연장하지만 `expireAfterWrite=30분`은 연장하지 않는다.

따라서 계속 인기인 게시글도 최대 30분마다 원본 DB를 다시 확인한다. 목록에 포함됐다는 이유로 본문이나 댓글을 미리 적재하지 않는다.

### 상태

상태는 `expireAfterWrite=1분`만 사용한다. 상세 접근이나 5분 목록 교체로 TTL을 연장하지 않는다. 캐시가 만료된 뒤 처음 접근한 요청이 최신 상태를 다시 조회한다.

## 8. Weigher 정책

본문과 댓글은 DB `TEXT`이므로 동일한 엔트리 수라도 메모리 크기가 크게 다르다. `maximumSize` 대신 다음 값을 근사 weight로 계산한다.

```text
각 문자열 길이 × 2
+ 컬렉션/DTO/키의 보수적 고정 비용
```

이 weight는 실제 heap 바이트 측정값이 아니다. 큰 본문·댓글이 캐시 예산을 독점하지 않게 만드는 eviction 기준이다.

- 본문 전체 논리 weight: 약 4 MiB
- 개별 댓글 전체 논리 weight: 약 8 MiB
- 단일 엔트리가 전체 한도보다 크면 해당 엔트리를 캐시에 유지하지 않는다.

향후 본문과 댓글 요청 DTO에 엄격한 최대 글자 수 제한을 추가하면 실측 결과에 따라 `maximumSize` 방식으로 단순화할 수 있다.

## 9. 정합성 수준

| 데이터 | 허용되는 지연 |
|---|---|
| 인기 순위와 목록 | 정상적으로 최대 약 5분 |
| 본문·작성자·댓글 내용 | 변경 이벤트 커밋 후 즉시 제거, 누락 시 최대 30분 |
| 조회·좋아요·댓글 수 | 최대 1분 |
| 신고 1~5회 수치 | 최대 1분 |
| 신고 6회 블라인드 | 지연 허용 안 함 |

## 10. 비인기글 정책

현재는 비인기글 본문·상태·댓글을 캐시하지 않는다. 인기글은 최근 조회 데이터로 선정된 이미 정의된 hot set이며, 제한된 heap에서 캐시 우선순위를 명확히 할 수 있다.

비인기글은 기존 API와 동일한 projection 조회를 사용하되 캐시에 저장하지 않는다. 운영 지표에서 비인기글의 반복 조회율이 확인될 때 일반 hot-post 캐시를 별도로 검토한다.

## 11. 운영 확인 지표

- 캐시별 hit/miss rate
- 캐시별 eviction count와 weighted size
- 인기글 상세·댓글 DB 쿼리 수
- 목록 갱신 성공/실패와 소요 시간
- JVM heap 사용량과 GC 빈도
- 상태 캐시의 1분 지연이 사용자 경험에 미치는 영향
