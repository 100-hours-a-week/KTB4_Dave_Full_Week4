spring:
  application:
    name: Community
  config:
    import: optional:file:.env[.properties]
  datasource:
    url: jdbc:mysql://${DB_HOST}:${DB_PORT:3306}/${DB_NAME}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
  h2:
    console:
      enabled: true
      path: /h2-console
  sql:
    init:
      mode: always
  jwt:
    secret: ${JWT_SECRET:asdfjalskdjfoqiuweoriuasdfkjlakjfkjozioouosefe}


tokenExpire:
  refresh: 1800000
  access: 600000

logging:
  level:
    org.springframework.web.servlet.DispatcherServlet: TRACE
    org.springframework.web.servlet.mvc.method.annotation.RequestMappingHandlerMapping: TRACE
    org.springframework.web.method.annotation: TRACE
    org.springframework.web.servlet.mvc.method.annotation.ServletInvocableHandlerMethod: TRACE
    org.springframework.web.servlet.mvc.support.DefaultHandlerExceptionResolver: DEBUG
    org.springframework.web.bind: DEBUG
    org.springframework.validation: DEBUG
    org:
      springframework:
        jdbc:
          core:
            JdbcTemplate: DEBUG
            StatementCreatorUtils: TRACE